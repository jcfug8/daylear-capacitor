const SWIM_LANE_SURFACE_BASE =
  "border border-[var(--ion-color-light-shade)] bg-white shadow-sm";

/** White card surface for swim lane items. */
export const SWIM_LANE_SURFACE = `rounded-lg ${SWIM_LANE_SURFACE_BASE}`;

/** White card surface for swim lane headers (flat bottom edge). */
export const SWIM_LANE_HEADER_SURFACE = `rounded-t-lg rounded-b-none ${SWIM_LANE_SURFACE_BASE}`;
